import 'package:flutter/material.dart';

// Colors
const Color primaryColor = Color(0xFF007BFF);
const Color secondaryColor = Color(0xFF6C757D);
const Color backgroundColor = Color(0xFFF8F9FA);
const Color textColor = Color(0xFF212529);
const Color errorColor = Color(0xFFDC3545);

// Text Styles
const TextStyle titleTextStyle = TextStyle(
  fontSize: 24,
  fontWeight: FontWeight.bold,
  color: textColor,
);

const TextStyle subtitleTextStyle = TextStyle(
  fontSize: 18,
  color: textColor,
);

const TextStyle bodyTextStyle = TextStyle(
  fontSize: 16,
  color: textColor,
);

// Padding
const EdgeInsetsGeometry screenPadding = EdgeInsets.all(16.0);

// Sizes
const double buttonHeight = 50.0;

// Other Constants
const String appName = 'TodoApp';
